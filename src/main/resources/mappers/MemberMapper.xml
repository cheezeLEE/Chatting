<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.Chat_Project.mapper.MemberMapper">

	<select id="selectById" parameterType="string" resultType="com.project.Chat_Project.domain.MemberVO">
		SELECT * FROM member WHERE userId = #{userId}
	</select>
	
	<insert id="registerMember" parameterType="com.project.Chat_Project.domain.MemberVO">
		INSERT INTO member(userId, user_name, userPw, userGender, userEmail, auth)
		VALUES(#{userId}, #{user_name}, #{userPw}, #{userGender}, #{userEmail}, #{auth})
	</insert>

	<select id="searchId" parameterType="com.project.Chat_Project.domain.MemberVO" resultType="string">
		SELECT userId FROM member WHERE user_name = #{user_name} AND userEmail = #{userEmail}
	</select>

</mapper>